import { useState } from "react"; import "./App.css"; /* ============================================================ TYPES ============================================================ */ type GameMode = | "login" | "menu" | "step" | "big-test" | "advanced"; type Phase = | "lesson" | "test" | "memory"; type Level = { title: string; description: string; lessonCode: string; solution: string; explanation: string; }; type AdvancedLevel = { title: string; description: string; codeAlreadyProvided: string; targetCode: string; fullExpectedCode: string; explanation: string; }; /* ============================================================ BASIC LEVELS ============================================================ */ const levels: Level[] = [ { title: "Level 1 — Trigger the Pipeline", description: "Every GitHub Actions workflow needs to know when it should start. Learn how to trigger a workflow whenever code is pushed to the main branch.", lessonCode: `on: push: branches: - main`, solution: `on: push: branches: - main`, explanation: "The 'on' section defines when the workflow runs. Here, a push to the main branch starts the pipeline.", }, { title: "Level 2 — Create a Job", description: "A workflow contains jobs. Create a job called 'build', configure it to run on Ubuntu, and create its steps section.", lessonCode: `jobs: build: runs-on: ubuntu-latest steps:`, solution: `jobs: build: runs-on: ubuntu-latest steps:`, explanation: "The 'jobs' section contains the work your pipeline performs. The build job runs on an Ubuntu runner and contains the steps that will execute.", }, { title: "Level 3 — Checkout the Repository", description: "The runner needs your repository's source code before it can build or test anything. Use the GitHub checkout action and give the step a descriptive name.", lessonCode: `- name: Checkout uses: actions/checkout@v4`, solution: `- name: Checkout uses: actions/checkout@v4`, explanation: "The 'name' property gives the step a human-readable name. 'uses' tells GitHub Actions to use an existing action. The checkout action downloads your repository onto the runner.", }, { title: "Level 4 — Install Dependencies", description: "Before building a Node/React application, install its dependencies. Give the step a descriptive name and then run npm install.", lessonCode: `- name: Install dependencies run: npm install`, solution: `- name: Install dependencies run: npm install`, explanation: "The 'run' keyword executes a command on the runner. Giving the step a name makes the pipeline easier to understand.", }, { title: "Level 5 — Build the Application", description: "Now build your application using the build script from package.json.", lessonCode: `- name: Build application run: npm run build`, solution: `- name: Build application run: npm run build`, explanation: "npm run build executes the project's build script and creates the production build.", }, { title: "Level 6 — Run Tests", description: "A CI pipeline should test the application before it is deployed.", lessonCode: `- name: Run tests run: npm test`, solution: `- name: Run tests run: npm test`, explanation: "Running tests allows the pipeline to catch problems before the application reaches deployment.", }, { title: "Level 7 — Create the Deploy Job", description: "Create a second job called 'deploy'. Make it wait until the build job succeeds.", lessonCode: `deploy: needs: build runs-on: ubuntu-latest steps:`, solution: `deploy: needs: build runs-on: ubuntu-latest steps:`, explanation: "The 'needs' property creates a dependency. The deploy job waits for the build job to finish successfully.", }, { title: "Level 8 — Deploy", description: "The final step is deployment. Give it a descriptive name and run the deployment command.", lessonCode: `- name: Deploy application run: npm run deploy`, solution: `- name: Deploy application run: npm run deploy`, explanation: "This represents your deployment stage. In a real Azure pipeline, this could instead use an Azure deployment action.", }, ]; /* ============================================================ ADVANCED LEVELS ============================================================ */ const advancedLevels: AdvancedLevel[] = [ { title: "Advanced 1 — Setup .NET", description: "Your API is written in .NET. Before restoring, building or testing it, the runner needs the correct .NET SDK.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4`, targetCode: ` - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }}`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }}`, explanation: "actions/setup-dotnet installs and configures the requested .NET SDK. The 'with' section supplies configuration to the action. Here, the version comes from an environment variable.", }, { title: "Advanced 2 — Restore Dependencies", description: "Now that the .NET SDK is installed, restore the NuGet dependencies required by the project.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }}`, targetCode: ` - name: Restore dependencies run: dotnet restore`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore`, explanation: "dotnet restore downloads the NuGet packages needed by the project. This is normally done before building.", }, { title: "Advanced 3 — Release Build", description: "Build the application using the Release configuration. '--no-restore' prevents GitHub Actions from restoring the dependencies again.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore`, targetCode: ` - name: Build run: dotnet build --configuration Release --no-restore`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore`, explanation: "The Release configuration creates an optimized production build. '--no-restore' tells dotnet build that dependency restoration has already happened.", }, { title: "Advanced 4 — Test the API", description: "After building, run the automated tests. '--no-build' prevents another unnecessary build.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore`, targetCode: ` - name: Test run: dotnet test --configuration Release --no-build`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build`, explanation: "dotnet test runs the project's automated tests. '--no-build' means the already-created build is reused.", }, { title: "Advanced 5 — Publish the API", description: "The application passed its tests. Now publish it into a folder called ./publish so it can later be deployed.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build`, targetCode: ` - name: Publish run: dotnet publish --configuration Release --no-build --output ./publish`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build - name: Publish run: dotnet publish --configuration Release --no-build --output ./publish`, explanation: "dotnet publish prepares the application for deployment. The --output argument specifies where the published files should be placed.", }, { title: "Advanced 6 — Upload an Artifact", description: "The build job needs to pass the published files to another job. Upload the ./publish folder as an artifact called 'api'.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build - name: Publish run: dotnet publish --configuration Release --no-build --output ./publish`, targetCode: ` - name: Upload artifact uses: actions/upload-artifact@v4 with: name: api path: ./publish`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build - name: Publish run: dotnet publish --configuration Release --no-build --output ./publish - name: Upload artifact uses: actions/upload-artifact@v4 with: name: api path: ./publish`, explanation: "Artifacts allow one job to store files so another job can download them later. Here the published API is stored as an artifact named 'api'.", }, { title: "Advanced 7 — Connect the Deploy Job", description: "Create the deploy job. It must wait until the build job succeeds.", codeAlreadyProvided: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build - name: Publish run: dotnet publish --configuration Release --no-build --output ./publish - name: Upload artifact uses: actions/upload-artifact@v4 with: name: api path: ./publish`, targetCode: ` deploy: runs-on: ubuntu-latest needs: build`, fullExpectedCode: `jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Setup .NET uses: actions/setup-dotnet@v4 with: dotnet-version: \${{ env.DOTNET_VERSION }} - name: Restore dependencies run: dotnet restore - name: Build run: dotnet build --configuration Release --no-restore - name: Test run: dotnet test --configuration Release --no-build - name: Publish run: dotnet publish --configuration Release --no-build --output ./publish - name: Upload artifact uses: actions/upload-artifact@v4 with: name: api path: ./publish deploy: runs-on: ubuntu-latest needs: build`, explanation: "The deploy job is a separate job. 'needs: build' means it will only run after the build job has completed successfully.", }, { title: "Advanced 8 — Download the Artifact", description: "The deploy job runs on a different runner. Download the artifact created by the build job.", codeAlreadyProvided: ` deploy: runs-on: ubuntu-latest needs: build steps:`, targetCode: ` - name: Download artifact uses: actions/download-artifact@v4 with: name: api path: ./publish`, fullExpectedCode: ` deploy: runs-on: ubuntu-latest needs: build steps: - name: Download artifact uses: actions/download-artifact@v4 with: name: api path: ./publish`, explanation: "download-artifact retrieves the files stored by upload-artifact. The name must match the artifact created by upload-artifact.", }, { title: "Advanced 9 — Login to Azure", description: "Before deploying to Azure, GitHub Actions needs to authenticate with your Azure account. The credentials are stored securely as a GitHub secret.", codeAlreadyProvided: ` deploy: runs-on: ubuntu-latest needs: build steps: - name: Download artifact uses: actions/download-artifact@v4 with: name: api path: ./publish`, targetCode: ` - name: Login to Azure uses: azure/login@v2 with: creds: \${{ secrets.AZURE_CREDENTIALS }}`, fullExpectedCode: ` deploy: runs-on: ubuntu-latest needs: build steps: - name: Download artifact uses: actions/download-artifact@v4 with: name: api path: ./publish - name: Login to Azure uses: azure/login@v2 with: creds: \${{ secrets.AZURE_CREDENTIALS }}`, explanation: "azure/login authenticates the workflow with Azure. The credentials come from GitHub Secrets, so you don't put the actual credentials directly into the YAML file.", }, { title: "Advanced 10 — Deploy to Azure App Service", description: "You've reached the deployment stage. Deploy the published API to an Azure App Service using the webapps-deploy action.", codeAlreadyProvided: ` deploy: runs-on: ubuntu-latest needs: build steps: - name: Download artifact uses: actions/download-artifact@v4 with: name: api path: ./publish - name: Login to Azure uses: azure/login@v2 with: creds: \${{ secrets.AZURE_CREDENTIALS }}`, targetCode: ` - name: Deploy to Azure App Service uses: azure/webapps-deploy@v3 with: app-name: \${{ env.AZURE_WEBAPP_NAME }} package: ./publish`, fullExpectedCode: ` deploy: runs-on: ubuntu-latest needs: build steps: - name: Download artifact uses: actions/download-artifact@v4 with: name: api path: ./publish - name: Login to Azure uses: azure/login@v2 with: creds: \${{ secrets.AZURE_CREDENTIALS }} - name: Deploy to Azure App Service uses: azure/webapps-deploy@v3 with: app-name: \${{ env.AZURE_WEBAPP_NAME }} package: ./publish`, explanation: "azure/webapps-deploy deploys the published files to an Azure App Service. app-name identifies the Web App and package tells the action which files to deploy.", }, ]; /* ============================================================ BASIC PIPELINE ============================================================ */ function getBasicPipeline(): string { return `on: push: branches: - main jobs: build: runs-on: ubuntu-latest steps: - name: Checkout uses: actions/checkout@v4 - name: Install dependencies run: npm install - name: Build application run: npm run build - name: Run tests run: npm test deploy: needs: build runs-on: ubuntu-latest steps: - name: Deploy application run: npm run deploy`; } /* ============================================================ YAML STRUCTURE CHECK ============================================================ */ type ParsedLine = { indent: number; content: string; }; function parseYamlStructure( code: string ): ParsedLine[] { return code .replace(/\r\n/g, "\n") .replace(/\r/g, "\n") .split("\n") .filter((line) => line.trim() !== "") .map((line) => ({ indent: line.length - line.trimStart().length, content: line.trim(), })); } function yamlMatches( userCode: string, expectedCode: string ): boolean { const user = parseYamlStructure(userCode); const expected = parseYamlStructure(expectedCode); if (user.length !== expected.length) { return false; } for (let i = 0; i < expected.length; i++) { if (user[i].content !== expected[i].content) { return false; } if (i === 0) { continue; } const userPrevious = user[i - 1].indent; const expectedPrevious = expected[i - 1].indent; const userDifference = user[i].indent - userPrevious; const expectedDifference = expected[i].indent - expectedPrevious; if ( userDifference === 0 && expectedDifference !== 0 ) { return false; } if ( userDifference > 0 && expectedDifference <= 0 ) { return false; } if ( userDifference < 0 && expectedDifference >= 0 ) { return false; } } return true; } /* ============================================================ APP ============================================================ */ function App() { /* * Start on the login screen. */ const [mode, setMode] = useState<GameMode>("login"); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [loginError, setLoginError] = useState(""); const [currentLevel, setCurrentLevel] = useState(0); const [phase, setPhase] = useState<Phase>("lesson"); const [code, setCode] = useState(""); const [result, setResult] = useState< "correct" | "wrong" | null >(null); const [errorMessage, setErrorMessage] = useState(""); const [completed, setCompleted] = useState(false); /* ========================================================== LOGIN ========================================================== */ function handleLogin() { if ( username === "admin" && password === "admin" ) { setLoginError(""); setPassword(""); setMode("menu"); return; } setLoginError( "Invalid username or password." ); } function handleLogout() { resetGame(); setUsername(""); setPassword(""); setLoginError(""); setMode("login"); } /* ========================================================== RESET GAME ========================================================== */ function resetGame() { setCurrentLevel(0); setPhase("lesson"); setCode(""); setResult(null); setErrorMessage(""); setCompleted(false); } /* ========================================================== OPEN MENU ========================================================== */ function openMenu() { setMode("menu"); resetGame(); } /* ========================================================== START STEP MODE ========================================================== */ function startStepMode() { resetGame(); setMode("step"); } /* ========================================================== START BIG TEST ========================================================== */ function startBigTest() { resetGame(); setMode("big-test"); setPhase("test"); } /* ========================================================== START ADVANCED ========================================================== */ function startAdvanced() { resetGame(); setMode("advanced"); } /* ========================================================== START INDIVIDUAL BASIC TEST ========================================================== */ function startLevelTest() { setPhase("test"); setCode(""); setResult(null); setErrorMessage(""); } /* ========================================================== NEXT BASIC LEVEL ========================================================== */ function nextBasicLevel() { const completedLevels = currentLevel + 1; if ( completedLevels % 2 === 0 && completedLevels < levels.length ) { setCurrentLevel(completedLevels); setPhase("memory"); setCode(""); setResult(null); setErrorMessage(""); return; } if ( completedLevels >= levels.length ) { setCompleted(true); return; } setCurrentLevel(completedLevels); setPhase("lesson"); setCode(""); setResult(null); setErrorMessage(""); } /* ========================================================== CHECK BASIC LEVEL ========================================================== */ function checkBasicLevel() { if ( yamlMatches( code, levels[currentLevel].solution ) ) { setResult("correct"); setTimeout( nextBasicLevel, 700 ); return; } setResult("wrong"); setErrorMessage( getBasicError( code, currentLevel ) ); } /* ========================================================== BIG TEST ========================================================== */ function checkBigTest() { if ( yamlMatches( code, getBasicPipeline() ) ) { setResult("correct"); setTimeout(() => { setCompleted(true); }, 700); return; } setResult("wrong"); setErrorMessage( "Your complete pipeline doesn't match what you learned in the basic course." ); } /* ========================================================== MEMORY TEST ========================================================== */ function getMemoryPipeline( completedLevels: number ) { let pipeline = ""; if (completedLevels >= 1) { pipeline += `on: push: branches: - main`; } if (completedLevels >= 2) { pipeline += ` jobs: build: runs-on: ubuntu-latest steps:`; } if (completedLevels >= 3) { pipeline += ` - name: Checkout uses: actions/checkout@v4`; } if (completedLevels >= 4) { pipeline += ` - name: Install dependencies run: npm install`; } if (completedLevels >= 5) { pipeline += ` - name: Build application run: npm run build`; } if (completedLevels >= 6) { pipeline += ` - name: Run tests run: npm test`; } if (completedLevels >= 7) { pipeline += ` deploy: needs: build runs-on: ubuntu-latest steps:`; } if (completedLevels >= 8) { pipeline += ` - name: Deploy application run: npm run deploy`; } return pipeline; } function checkMemory() { const expected = getMemoryPipeline( currentLevel ); if ( yamlMatches( code, expected ) ) { setResult("correct"); setTimeout(() => { if ( currentLevel >= levels.length ) { setCompleted(true); return; } setCurrentLevel( currentLevel ); setPhase("lesson"); setCode(""); setResult(null); }, 700); return; } setResult("wrong"); setErrorMessage( "Your pipeline doesn't contain everything you've learned so far." ); } /* ============================================================ ADVANCED TEST ============================================================ */ function checkAdvanced() { const level = advancedLevels[ currentLevel ]; if ( yamlMatches( code, level.fullExpectedCode ) ) { setResult("correct"); setTimeout(() => { if ( currentLevel + 1 >= advancedLevels.length ) { setCompleted(true); return; } setCurrentLevel( currentLevel + 1 ); setPhase("lesson"); setCode(""); setResult(null); setErrorMessage(""); }, 700); return; } setResult("wrong"); setErrorMessage( getAdvancedError( code, level ) ); } /* ============================================================ ADVANCED ERROR ============================================================ */ function getAdvancedError( submitted: string, level: AdvancedLevel ): string { const user = parseYamlStructure( submitted ); if ( user.length === 0 ) { return "You didn't enter anything."; } const target = parseYamlStructure( level.targetCode ); const missing = target.find( (line) => !user.some( (userLine) => userLine.content === line.content ) ); if (missing) { return `You're missing this line: ${missing.content}`; } return "The new code is present, but its structure or indentation is incorrect."; } /* ============================================================ BASIC ERROR ============================================================ */ function getBasicError( submitted: string, index: number ): string { const user = parseYamlStructure( submitted ); if ( user.length === 0 ) { return "You didn't enter any YAML."; } const expected = parseYamlStructure( levels[index].solution ); const missing = expected.find( (line) => !user.some( (userLine) => userLine.content === line.content ) ); if (missing) { return `You're missing: ${missing.content}`; } return "The code content is present, but the YAML structure or indentation is incorrect."; } /* ============================================================ ADVANCED LESSON → TEST ============================================================ */ function startAdvancedTest() { setPhase("test"); setResult(null); setErrorMessage(""); const level = advancedLevels[ currentLevel ]; setCode( level.codeAlreadyProvided ); } /* ============================================================ CONTINUE AFTER FAILURE ============================================================ */ function continueAfterError() { if ( mode === "advanced" ) { setCurrentLevel(0); setPhase("lesson"); setCode(""); setResult(null); setErrorMessage(""); return; } setCurrentLevel(0); setPhase( mode === "big-test" ? "test" : "lesson" ); setCode(""); setResult(null); setErrorMessage(""); } /* ============================================================ TAB SUPPORT ============================================================ */ function handleEditorKeyDown( event: React.KeyboardEvent<HTMLTextAreaElement> ) { if ( event.key !== "Tab" ) { return; } event.preventDefault(); const textarea = event.currentTarget; const start = textarea.selectionStart; const end = textarea.selectionEnd; const newCode = code.substring(0, start) + " " + code.substring(end); setCode(newCode); requestAnimationFrame(() => { textarea.selectionStart = start + 2; textarea.selectionEnd = start + 2; }); } /* ============================================================ LOGIN SCREEN ============================================================ */ if ( mode === "login" ) { return ( <div className="login-page"> <div className="login-card"> <div className="login-icon"> 🚀 </div> <div className="eyebrow"> CI/CD TRAINING </div> <h1> Pipeline Builder </h1> <p className="login-subtitle"> Sign in to access the training environment. </p> <div className="login-form"> <label htmlFor="username"> Username </label> <input id="username" type="text" value={username} onChange={(event) => { setUsername( event.target.value ); setLoginError(""); }} placeholder="Enter username" autoComplete="username" autoFocus /> <label htmlFor="password"> Password </label> <input id="password" type="password" value={password} onChange={(event) => { setPassword( event.target.value ); setLoginError(""); }} onKeyDown={(event) => { if ( event.key === "Enter" ) { handleLogin(); } }} placeholder="Enter password" autoComplete="current-password" /> {loginError && ( <div className="login-error"> <span>✕</span> {loginError} </div> )} <button className="login-button" onClick={handleLogin} disabled={ !username || !password } > Sign In → </button> </div> <div className="login-footer"> GitHub Actions Training Environment </div> </div> </div> ); } /* ============================================================ MENU ============================================================ */ if ( mode === "menu" ) { return ( <div className="game"> <div className="game-card menu-card"> <div className="menu-content"> <div className="eyebrow"> CI/CD TRAINING </div> <h1> Pipeline Builder </h1> <p className="menu-subtitle"> Choose how you want to train. </p> <div className="mode-grid"> <ModeCard icon="📚" title="Step by Step" description="Learn one concept at a time, practice it, and periodically rebuild everything you've learned from memory." onClick={ startStepMode } /> <ModeCard icon="🧠" title="Big Test" description="Skip the lessons and write the entire basic CI/CD pipeline from memory." onClick={ startBigTest } /> <ModeCard icon="⚡" title="Advanced" description="The basic pipeline is already there. Practice real .NET, artifacts and Azure deployment syntax." onClick={ startAdvanced } /> </div> <div className="menu-tip"> <span> 💡 </span> <p> <strong> Recommended: </strong>{" "} Start with Step by Step, then use Big Test to check what you remember, and finally move on to Advanced. </p> </div> <button className="logout-button" onClick={handleLogout} > Log out </button> </div> </div> </div> ); } /* ============================================================ COMPLETED ============================================================ */ if (completed) { return ( <div className="game"> <div className="game-card completion-card"> <div className="completion-icon"> 🚀 </div> <div className="eyebrow"> PIPELINE SUCCESS </div> <h1> {mode === "advanced" ? "Advanced Pipeline Complete!" : "CI/CD Pipeline Complete!"} </h1> <p className="completion-description"> {mode === "advanced" ? "You successfully worked through the advanced .NET and Azure pipeline concepts." : "You successfully built the entire pipeline and passed the test."} </p> <button className="primary-button" onClick={ openMenu } > Back to Menu </button> </div> </div> ); } /* ============================================================ ADVANCED MODE ============================================================ */ if ( mode === "advanced" ) { const level = advancedLevels[ currentLevel ]; return ( <> <GameLayout title="Advanced Pipeline" level={ currentLevel + 1 } total={ advancedLevels.length } onMenu={ openMenu } > {phase === "lesson" && ( <section className="lesson"> <div className="advanced-badge"> ⚡ ADVANCED </div> <div className="step-label"> ADVANCED LEVEL{" "} {currentLevel + 1} </div> <h2> {level.title} </h2> <p className="description"> {level.description} </p> <div className="learn-box"> <div className="box-title"> 📖 WHAT YOU ALREADY HAVE </div> <div className="code-example"> <div className="code-header"> <span> ci.yml </span> <span> YAML </span> </div> <pre> {level.codeAlreadyProvided} </pre> </div> <div className="new-code-box"> <div className="new-code-title"> ⚡ ADD THIS </div> <pre> {level.targetCode} </pre> </div> <div className="explanation"> <strong> What does it do? </strong> <p> {level.explanation} </p> </div> </div> <button className="primary-button understand-button" onClick={ startAdvancedTest } > OK, I Understand → </button> </section> )} {phase === "test" && ( <section className="test"> <div className="advanced-badge"> ⚡ ADVANCED </div> <div className="step-label"> PRACTICE — LEVEL{" "} {currentLevel + 1} </div> <h2> Add the new code </h2> <p className="description"> The code you've already learned is provided. Add the new section you just learned. </p> <Editor code={code} setCode={setCode} onKeyDown={ handleEditorKeyDown } full placeholder="Add the new YAML here..." /> <button className="primary-button check-button" onClick={ checkAdvanced } disabled={ !code.trim() } > ▶ Check Answer </button> {result === "correct" && ( <div className="feedback correct"> <div className="feedback-icon"> ✓ </div> <div> <strong> Correct! </strong> <p> Moving to the next advanced concept... </p> </div> </div> )} </section> )} </GameLayout> {result === "wrong" && ( <FailureTerminal code={code} expected={ level.fullExpectedCode } error={ errorMessage } onContinue={ continueAfterError } /> )} </> ); } /* ============================================================ BIG TEST ============================================================ */ if ( mode === "big-test" ) { return ( <> <GameLayout title="Big Test" level={1} total={1} onMenu={ openMenu } > <section className="full-test"> <div className="memory-test-banner"> 🧠 FINAL BASIC TEST </div> <div className="step-label"> NO HINTS </div> <h2> Build the entire pipeline </h2> <p className="description"> You skipped the lessons. Now prove that you can write the complete basic CI/CD pipeline from memory. </p> <div className="memory-info"> <div> <span> EXPECTED </span> <strong> COMPLETE PIPELINE </strong> </div> <div> <span> HINTS </span> <strong> NONE </strong> </div> </div> <Editor code={code} setCode={setCode} onKeyDown={ handleEditorKeyDown } full placeholder="Write the complete pipeline..." /> <button className="primary-button check-button" onClick={ checkBigTest } disabled={ !code.trim() } > 🚀 Submit Pipeline </button> </section> </GameLayout> {result === "wrong" && ( <FailureTerminal code={code} expected={ getBasicPipeline() } error={ errorMessage } onContinue={ continueAfterError } /> )} </> ); } /* ============================================================ STEP-BY-STEP ============================================================ */ return ( <> <GameLayout title="Step by Step" level={ phase === "memory" ? currentLevel : currentLevel + 1 } total={ levels.length } onMenu={ openMenu } > {phase === "lesson" && ( <section className="lesson"> <div className="step-label"> STEP{" "} {currentLevel + 1} </div> <h2> { levels[ currentLevel ].title } </h2> <p className="description"> { levels[ currentLevel ].description } </p> <div className="learn-box"> <div className="box-title"> 📖 LEARN THIS </div> <p className="learn-description"> Study the code below. You will need to reproduce it yourself. </p> <div className="code-example"> <div className="code-header"> <span> .github/workflows/ci.yml </span> <span> YAML </span> </div> <pre> { levels[ currentLevel ].lessonCode } </pre> </div> <div className="explanation"> <strong> What does it do? </strong> <p> { levels[ currentLevel ].explanation } </p> </div> </div> <button className="primary-button understand-button" onClick={ startLevelTest } > OK, I Understand → </button> </section> )} {phase === "test" && ( <section className="test"> <div className="step-label"> TEST — LEVEL{" "} {currentLevel + 1} </div> <h2> Now write it yourself </h2> <p className="description"> Without looking at the lesson, write the YAML required for this level. </p> <Editor code={code} setCode={setCode} onKeyDown={ handleEditorKeyDown } placeholder="Write your YAML here..." /> <button className="primary-button check-button" onClick={ checkBasicLevel } disabled={ !code.trim() } > ▶ Check Answer </button> {result === "correct" && ( <div className="feedback correct"> <div className="feedback-icon"> ✓ </div> <div> <strong> Correct! </strong> <p> Loading the next stage... </p> </div> </div> )} </section> )} {phase === "memory" && ( <section className="full-test"> <div className="memory-test-banner"> 🧠 MEMORY TEST </div> <div className="step-label"> REBUILD WHAT YOU KNOW </div> <h2> Write the entire pipeline </h2> <p className="description"> You've completed{" "} <strong> {currentLevel} </strong>{" "} levels. <br /> Rebuild everything you've learned up to this point from memory. </p> <div className="memory-info"> <div> <span> COMPLETED </span> <strong> LEVELS 1–{currentLevel} </strong> </div> <div> <span> REMEMBER </span> <strong> EVERYTHING SO FAR </strong> </div> </div> <Editor code={code} setCode={setCode} onKeyDown={ handleEditorKeyDown } full placeholder="Rebuild the pipeline from memory..." /> <button className="primary-button check-button" onClick={ checkMemory } disabled={ !code.trim() } > 🚀 Submit Full Pipeline </button> </section> )} </GameLayout> {result === "wrong" && ( <FailureTerminal code={code} expected={ phase === "memory" ? getMemoryPipeline( currentLevel ) : levels[ currentLevel ].solution } error={ errorMessage } onContinue={ continueAfterError } /> )} </> ); } /* ============================================================ GAME LAYOUT ============================================================ */ function GameLayout({ children, title, level, total, onMenu, }: { children: React.ReactNode; title: string; level: number; total: number; onMenu: () => void; }) { return ( <div className="game"> <div className="game-card"> <header className="header"> <div> <div className="eyebrow"> CI/CD TRAINING </div> <h1> {title} </h1> <p> Learn it. Write it. Remember it. </p> </div> <div className="header-right"> <button className="menu-button" onClick={ onMenu } > ☰ Menu </button> <div className="level-counter"> <span> PROGRESS </span> <strong> {level} <small> /{total} </small> </strong> </div> </div> </header> <div className="progress-container"> <div className="progress" style={{ width: `${Math.min( (level / total) * 100, 100 )}%`, }} /> </div> <main> {children} </main> <footer> <span> GitHub Actions Training </span> <span> Use TAB for indentation </span> </footer> </div> </div> ); } /* ============================================================ MODE CARD ============================================================ */ function ModeCard({ icon, title, description, onClick, }: { icon: string; title: string; description: string; onClick: () => void; }) { return ( <button className="mode-card" onClick={onClick} > <div className="mode-icon"> {icon} </div> <div className="mode-card-content"> <h2> {title} </h2> <p> {description} </p> </div> <span className="mode-arrow"> → </span> </button> ); } /* ============================================================ EDITOR ============================================================ */ function Editor({ code, setCode, onKeyDown, placeholder, full = false, }: { code: string; setCode: (value: string) => void; onKeyDown: ( event: React.KeyboardEvent<HTMLTextAreaElement> ) => void; placeholder: string; full?: boolean; }) { const lineCount = Math.max( code.split("\n").length, 1 ); return ( <div className="editor"> <div className="editor-top"> <div className="window-buttons"> <span /> <span /> <span /> </div> <span> .github/workflows/ci.yml </span> </div> <div className={ full ? "editor-body full-editor" : "editor-body" } > <div className="line-numbers"> {Array.from( { length: lineCount, }, (_, index) => ( <div key={index} > {index + 1} </div> ) )} </div> <textarea autoFocus value={code} onChange={(event) => setCode( event.target.value ) } onKeyDown={ onKeyDown } spellCheck={false} placeholder={ placeholder } /> </div> <div className="editor-bottom"> <span> YAML </span> <span> UTF-8 </span> </div> </div> ); } /* ============================================================ FAILURE TERMINAL ============================================================ */ function FailureTerminal({ code, expected, error, onContinue, }: { code: string; expected: string; error: string; onContinue: () => void; }) { return ( <div className="modal-overlay"> <div className="terminal-window"> <div className="terminal-header"> <div className="terminal-buttons"> <span /> <span /> <span /> </div> <div className="terminal-title"> pipeline-validator </div> </div> <div className="terminal-content"> <div className="terminal-command"> <span className="terminal-green"> user@pipeline </span> <span className="terminal-white"> :~$ </span>{" "} github-actions-validator ci.yml </div> <div className="terminal-failed"> ✗ PIPELINE FAILED </div> <div className="terminal-message"> {error} </div> <div className="terminal-section"> <div className="terminal-section-title terminal-red"> YOUR CODE </div> <TerminalCode code={code} /> </div> <div className="terminal-section"> <div className="terminal-section-title terminal-green-text"> EXPECTED CODE </div> <TerminalCode code={expected} /> </div> <div className="terminal-error"> <span className="terminal-red"> ERROR: </span> <span> {error} </span> </div> <div className="terminal-prompt"> <span className="terminal-green"> user@pipeline </span> <span className="terminal-white"> :~$ </span> <span className="terminal-cursor"> _ </span> </div> <button className="terminal-continue" onClick={ onContinue } > CONTINUE </button> </div> </div> </div> ); } /* ============================================================ TERMINAL CODE ============================================================ */ function TerminalCode({ code, }: { code: string; }) { return ( <div className="terminal-code"> {code ? code .split("\n") .map( ( line, index ) => ( <div className="terminal-line" key={index} > <span className="terminal-line-number"> {String( index + 1 ).padStart( 2, "0" )} </span> <span> {line || " "} </span> </div> ) ) : ( <div className="terminal-empty"> [no code submitted] </div> )} </div> ); } export default App;